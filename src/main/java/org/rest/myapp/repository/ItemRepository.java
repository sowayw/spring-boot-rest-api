package org.rest.myapp.repository;

// TODO: This class will be refactored and used for future features(maybe).

/*

 import java.util.List;

 import org.rest.myapp.model.entity.Item;

 import org.springframework.data.jpa.repository.JpaRepository;
 import org.springframework.data.jpa.repository.Query;

 public interface ItemRepository extends JpaRepository<Item, Long> {
@Query("""
            SELECT i
            FROM Item i
            JOIN i.user u
            """)
    @Query("SELECT i FROM Item i JOIN i.user u")
    List<Item> findAllItemsWithUsers();
}
*/
